var slider = tns({
    container: ".carousel__inner",
    items: 1,
    slideBy: 'page',
    autoplay: false,
    // center: true,
    constrols: false,
    prevButton: ".slider-prev-btn",
    nextButton: ".slider-next-btn",
    nav: false,
    // navPosition: "bottom"
});